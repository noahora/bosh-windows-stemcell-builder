#!/usr/bin/env ruby

require 'fileutils'
require 'json'

packer_config = ARGV.last
content = File.read(packer_config)
output_dir = JSON.parse(content)['builders'][0]['output_directory']
FileUtils.mkdir_p(output_dir)
Dir.chdir(output_dir) do
  File.write("file.vmx", "banana")
end
